@using OnlineLibrary.Services.Models.Publisher
@model IEnumerable<PublisherAllDto>

@{
    ViewData["Title"] = "Manage Publishers";
}

<div class="container mt-4">
    <h1 class="text-center text-warning fw-bold">Manage Publishers</h1>
    <p class="text-center text-muted">View, edit, or delete publisher entries.</p>

    <div class="d-flex justify-content-between mb-3">
        <a asp-area="Admin" asp-controller="Home" asp-action="Index" class="btn btn-secondary fw-bold">
            <i class="bi bi-arrow-left"></i> Back to Dashboard
        </a>
        <a asp-area="Admin" asp-controller="PublisherManagement" asp-action="Add" class="btn btn-success fw-bold shadow-lg">
            <i class="bi bi-plus-circle"></i> Add New Publisher
        </a>
    </div>

    @if (!Model.Any())
    {
        <div class="alert alert-info text-center" role="alert">
            <i class="bi bi-exclamation-circle"></i> No publishers found. Click "Add New Publisher" to create one.
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-dark table-hover shadow-lg rounded-3">
                <thead>
                    <tr class="text-warning text-center">
                        <th scope="col">Name</th>
                        <th scope="col">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var publisher in Model)
                    {
                        <tr class="align-middle text-center">
                            <td class="fw-bold">@publisher.Name</td>
                            <td>
                                <a asp-area="Admin" asp-controller="PublisherManagement" asp-action="Edit" asp-route-id="@publisher.Id"
                                   class="btn btn-sm btn-warning me-1">
                                    <i class="bi bi-pencil-fill"></i> Edit
                                </a>
                                <a asp-area="Admin" asp-controller="PublisherManagement" asp-action="Delete" asp-route-id="@publisher.Id"
                                   class="btn btn-sm btn-danger">
                                    <i class="bi bi-trash-fill"></i> Delete
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>
